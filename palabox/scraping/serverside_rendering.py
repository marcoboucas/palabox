"""Scraping for serverside rendering."""

from typing import Optional

import requests


def scrap_one_website(session: Optional[requests.Session] = None) -> str:
    """Scrap one website."""

    return ""
